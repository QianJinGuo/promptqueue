services:
  promptqueue:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "9090:9090"
    volumes:
      - ../data:/root/.promptqueue
    env_file:
      - ../.env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
